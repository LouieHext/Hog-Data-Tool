INPUT_DATA_PATH=./data/hog_data.csv
OUTPUT_DATA_PATH=./data/outputs/
WEIGHT_UNIT=lbs
# Optional: path to a CSV or folder of CSVs with date_time, reps, rest, weight, max_hold, side, gripper (grippers loaded from column)
# ALT_DATA_FOLDER=./data/alt/